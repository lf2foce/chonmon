<% pre_like = @post.likes.find { |like| like.user_id == current_user.id} %>
<% @like = current_user.likes.find_by(post_id: @post.id) %>
<%# like = @post.likes.find(params[:like_id]) %>
<%= link_to post_like_path(@post, @like), method: :delete, remote: true, class: "grab" do %>

<span style="font-size: 1.5em; color: #00AD39;">
   <i class="fas fa-heart"></i>
</span>  <span style="text-decoration: none; color: #00AD39;">Like</span>


<% end %>
<%# @like = current_user.likes.find_by(post_id: @post.id) %>
<%#= link_to "Unlike", post_like_path(@post, @like), method: :delete, remote: true %>