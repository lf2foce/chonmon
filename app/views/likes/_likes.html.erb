<% if user_signed_in? && current_user.likes?(@post) %>

<%= link_to "Unlike", post_like_path(@post, @like), method: :delete, remote: true %>

<% else %>

<%= link_to "Like", post_likes_path(@post), method: :post, remote: true %>

<% end %>


<%= @post.likes.count  %> + ahha